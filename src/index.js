import React from "react";
import ReactDOM from "react-dom/client";
import App from "./Components/App/App";

const root = ReactDOM.createRoot(document.querySelectorAll(".root"));
root.render(<App />);
